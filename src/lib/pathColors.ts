import type { PlayerPath } from '@/types/play'

export const PATH_COLOR: Record<PlayerPath['type'], string> = {
  primary: '#fbbf24',
  secondary: '#93c5fd',
  clear: '#a3a3a3',
  reset: '#f472b6',
}
